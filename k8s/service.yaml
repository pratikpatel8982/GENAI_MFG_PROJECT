apiVersion: v1
kind: Service
metadata:
  name: manufacturing-agent-service
spec:
  type: NodePort
  selector:
    app: manufacturing-agent
  ports:
    - port: 80
      targetPort: 8000
      nodePort: 30007
